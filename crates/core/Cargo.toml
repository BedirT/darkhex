[package]
name = "darkhex-core"
version = "0.1.0"
edition = "2021"
description = "Pure Rust game engine and solvers for Dark Hex"
license = "MIT"

[dependencies]
rand = { version = "0.8", features = ["small_rng"] }
serde = { version = "1", features = ["derive"] }
bincode = "1"
